# Скопируй в values.secrets.yaml и заполни.
# Установка: helm upgrade --install llm-rag . -f values.yaml -f values.secrets.yaml
secrets:
  langfusePublicKey: "pk-lf-..."
  langfuseSecretKey: "sk-lf-..."
  hfToken: "hf_..."
  clearmlAccessKey: ""
  clearmlSecretKey: ""
